# Copy to .env in the repo root and adjust values

# Redis
REDIS_PASSWORD=admin

# Auth service secrets
PEPPER=change-me-pepper
ADMIN_USER=admin
ADMIN_PASS=admin

# Optional defaults
TOKEN_TTL_SECONDS=36000000
RATE_LIMIT_WINDOW_SEC=1
RATE_LIMIT_MAX=20
