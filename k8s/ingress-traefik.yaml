---
# Note: auth-service admin UI is NOT exposed externally for security reasons
# Access it via port-forward: kubectl port-forward svc/auth-service 8000:8000
# Then visit: http://localhost:8000
---
# Example: Protected service with auth middleware
# Uncomment and modify for your services
# apiVersion: networking.k8s.io/v1
# kind: Ingress
# metadata:
#   name: my-protected-service
#   namespace: default
#   annotations:
#     traefik.ingress.kubernetes.io/router.middlewares: default-auth-middleware@kubernetescrd
# spec:
#   ingressClassName: traefik
#   rules:
#     - host: myapp.trofkm.ru
#       http:
#         paths:
#           - path: /
#             pathType: Prefix
#             backend:
#               service:
#                 name: my-service
#                 port:
#                   number: 80
