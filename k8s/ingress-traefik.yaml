---
# Admin UI is not exposed externally. Use port-forward:
#   kubectl port-forward svc/auth-service 8000:8000
# Then visit http://localhost:8000
---
# Example: Protected service with auth middleware
# Uncomment and modify for your services
# apiVersion: networking.k8s.io/v1
# kind: Ingress
# metadata:
#   name: my-protected-service
#   namespace: default
#   annotations:
#     traefik.ingress.kubernetes.io/router.middlewares: default-auth-middleware@kubernetescrd
# spec:
#   ingressClassName: traefik
#   rules:
#     - host: myapp.example.com
#       http:
#         paths:
#           - path: /
#             pathType: Prefix
#             backend:
#               service:
#                 name: my-service
#                 port:
#                   number: 80
